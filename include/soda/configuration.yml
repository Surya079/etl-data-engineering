data_source pg_datasource:
  type: postgres
  username: ${ELT_DATABASE_USERNAME}
  password: ${ELT_DATABASE_PASSWORD}  # without quotes
  host: postgres
  port: "5432"
  database: elt_db
  schema: ${SCHEMA}